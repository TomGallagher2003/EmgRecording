# Constants

The app uses a few small constants:

- `SIZE = 135`
- `WINDOW_WIDTH = 10 * SIZE`
- `WINDOW_HEIGHT = 5 * SIZE`
- `INITIAL_BASELINE_SECONDS = 4`

These live in `timer.py` today. If you centralize them later (e.g., `core/constants.py`), update this page accordingly.
