#: Base sizing unit used to derive window dimensions.
SIZE = 135

# Main window width in pixels (10 × SIZE).
WINDOW_WIDTH = 10 * SIZE

# Main window height in pixels (5 × SIZE).
WINDOW_HEIGHT = 5 * SIZE

# Duration (seconds) of the very first baseline rest.
INITIAL_BASELINE_SECONDS = 4

# Radial arc animation tick interval (milliseconds) for the PhaseManager/UI.
ARC_TICK_MS = 50

# Directory where recorded data files are written.
DATA_DESTINATION_PATH = "./data"

# Directory containing movement images (set A/B assets).
IMAGE_SOURCE_PATH = "./movement_library/EA"
